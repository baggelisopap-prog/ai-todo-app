# Development-only dependencies. Install with:
#   ./venv/Scripts/python.exe -m pip install -r requirements-dev.txt
#
# Deliberately NOT added to requirements.txt: that file ships to Render and
# is UTF-16LE with a BOM (see docs/ARCHITECTURE.md), so it is best left
# untouched. This file is plain UTF-8 and standalone — it does not `-r`
# requirements.txt, because pointing pip at a UTF-16 file through an
# include is the exact trap that encoding note exists to warn about.
pytest==8.3.4
