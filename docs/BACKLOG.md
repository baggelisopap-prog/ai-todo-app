# BACKLOG — deferred, not started
_Ideas parked deliberately. Moving one into active work = write it into CURRENT_TASK.md._

- **Integrations research (2026-08-09)** — ranked catalogue with depth, in `docs/superpowers/specs/2026-08-09-integrations-research.md`. TWO tracks, because property managers are a first-class audience: horizontal (email→task, Android share sheet, Telegram→WhatsApp, Outlook) and vertical STR (reservation events→tasks, Hosthub, Smoobu, Guesty, Turno/Breezeway). Three things worth knowing without reading it: **reminders, two-way calendar sync and email forwarding are all PAID at Todoist/TickTick and free here**; the paid line should meter AI volume rather than gate integrations, because unlike its competitors this app has a real marginal cost per use, with a separate per-property tier for managers; and **the PMS adapters must be refactored behind one reservation-event model at the SECOND connector, not later**. Distribution (Hostaway/Guesty marketplaces, and Hosthub being Athens-founded) is likely worth more than several of the connectors. myDATA e-invoicing deliberately refused.
- **Hostaway threading + conversation link + auto-complete on reply (2026-08-10)** — ~~design approved, not implemented~~ **IMPLEMENTED on `main`, live verification still open (see CURRENT_TASK.md)**: `docs/superpowers/specs/2026-08-10-hostaway-threading-design.md`. Verified against the live Hostaway API before designing, which is where the value is: `conversationId` exists on every message and the deep link is `dashboard.hostaway.com/messages/inbox/{conversationId}`; the reservation field names `get_reservation_details` has warned about since day one are **correct** (delete the warning); and the account has a `messageReceived` auto-reply that would have made the obvious version of auto-complete **close every task seconds after creating it**. The signal that survives is `userId` (null on all 66 automations, set on 23/24 human replies) — NOT `communicationId`, which a third-party GuestArrive message defeats. The 90-second threading window is measured from 141 real message pairs, not chosen. One unverified assumption left: whether the webhook fires for outgoing messages.
- **Email → task (2026-08-10)** — brainstormed, parked unfinished, in `docs/superpowers/specs/2026-08-10-email-to-task-notes.md`. Forwarding wins because the two nicer options (a Gmail add-on, or the app reading Gmail via the `gmail.readonly` restricted scope) both need Google review this project isn't ready to pay for. Three things to know without reading it: **it is blocked on the "Custom domain" item below**, which is a hard prerequisite here and not cosmetic — inbound mail needs MX records and neither Vercel nor Render can receive mail; **the note the user types above the forward is the feature**, not the email under it (and it lands on the right side of the existing data-vs-instructions rule); and the cost shape is not Todoist's — every email is a `gemini-3.5-flash` call, so the header/allow-list/HTML-stripping filters must run in code BEFORE the model. Stopped on one open question: what a forwarded email with no note should become.
- **Agent writes Phase 2**: delete tasks + Google Calendar event operations (higher risk than Phase 1's complete/update/create).
- **Task report export (Excel / PDF)** — e.g. "κάνε μου report με όλα τα business tasks του έτους". Design already thought through (2026-08-02), parked for later:
  - **Core principle**: the file is generated ENTIRELY in Python; the agent's only job is translating natural language into parameters (`date_from`, `date_to`, `category`, `include_completed`) — roughly 30 output tokens. Task rows never enter the model's context, so 500 tasks cost the same as 5, `MAX_SEARCH_RESULTS = 30` does not apply (it is a token guard and there are no tokens here), descriptions are not truncated to 100 chars, and the totals are arithmetically correct because Python counts them, not the LLM.
  - **Excel first, not PDF.** PDF has a concrete Greek-text trap: `reportlab`'s default fonts (Helvetica/Times) have no Greek glyphs, so it needs an embedded TTF (DejaVuSans/Noto, ~700KB committed to the repo); `weasyprint` needs cairo/pango system libs, awkward on Render. Excel has none of that (UTF-8 is fine) and is more useful anyway — the owner can filter/sort/pivot.
  - **Dependency needed**: nothing suitable is installed today (checked: no openpyxl / xlsxwriter / pandas / reportlab / weasyprint). `xlsxwriter` (zero transitive deps, write-only) or `openpyxl`. NOTE: `requirements.txt` is UTF-16LE with BOM — preserve the encoding when adding the line.
  - **Fits the existing propose-then-confirm plumbing**: a `propose_report(...)` tool appending `type: "report"` to `proposed_actions`, rendered as a card with a "Download Excel" button instead of Confirm, hitting a dedicated user-scoped endpoint that streams the file. Reuses the cards, the proposed_actions flow, and the server-side re-validation pattern.
  - **Open question — does this need the agent at all?** A "Reports" button in the UI with category + date-range pickers costs ZERO tokens forever and is more discoverable; an agent tool costs ~150-250 tokens on EVERY request whether used or not (compare: the three `propose_*` tools measured at 762). Suggested order: build the endpoint + UI button first (that's where the actual work is), then decide separately whether the natural-language layer earns its per-round cost.
- **Tiered search results** (raised alongside the report idea, also parked): return ~20 rows with description, then a further ~60-80 title-only rows, instead of a flat 30. Estimated ~75-90 tokens/row full vs ~35 with UUID vs ~21 without. "20 full + 25 slim" is both CHEAPER than today's 30 full and more complete; "20 full + 130 slim" is nearly double. Drop the UUID from the slim tier (~15 tokens/row — 2.000 tokens across 130 rows) and let the model re-search by name in the rare case it must act on a slim row. Largely obsoleted if the report export above ships, since aggregate questions stop needing wide result sets.
- **Hostaway encryption key: rotation, and one key for everyone (2026-08-14)** — two accepted weaknesses of the `HOSTAWAY_ENCRYPTION_KEY` design, both fine for one business and neither fine indefinitely (see DECISIONS.md). **Rotation**: if the key leaks, the correct response is a new key plus re-encrypting every `hostaway_connections` row, and nothing does that today — the key cannot currently be changed without breaking every stored secret, which also means a leak has no remediation short of every user reconnecting by hand. A small script (decrypt with old, encrypt with new, in one transaction) closes it. **One key for all**: the fifteen colleagues' secrets all encrypt under the same key, so it is a single point of compromise for the whole team; per-user keys, or a KMS, would be better and considerably more complex. Neither is urgent; both should be revisited before a second business connects.
- **Billing / quota**: `profiles.monthly_token_quota` + Stripe; check summed `token_usage_log` vs quota before AI calls; "approaching limit" notices.
- **Monitoring**: Sentry (error tracking, free ≤5k errors/mo) + UptimeRobot (uptime, free 50 monitors/5-min — note: free tier is "non-commercial", flag given the Hostaway business use). ~~Add a `/health` endpoint for UptimeRobot.~~ **`/health` already exists** (`main.py`, verified 2026-08-23) — what remains here is signing up for the two services, which is an owner action, not code.
- **Admin dashboard** (owner-only): view ALL users' activity/usage; extend the Developer token dashboard, gated by owner user_id. **Prerequisite when this starts**: `/dev/token-usage` is currently auth-gated only — it is self-scoped (`get_usage_summary(user_id)` uses the caller's own bearer-token user_id), so there is no cross-user leak today, and the "Developer" section is hidden by a FRONTEND-only owner check in `SettingsModal.jsx`. The moment this endpoint (or a new admin endpoint) returns other users' rows, a SERVER-SIDE owner check becomes mandatory — frontend hiding is not a security boundary.
- ~~**Agent conversation history UI** (browse/resume past conversations)~~ **BUILT 2026-08-23, not yet seen running.** Browse and audit only — **resume was deliberately dropped**: of the three things this could do, resuming is the only one that re-sends the old messages to the model and is paid for again on every turn, and the owner chose against it. Three endpoints (`GET /agent/conversations`, `GET /agent/conversations/{id}`, `POST /agent/action-cancelled`), a pure `agent_history.py` for the grouping, and `AgentHistoryView.jsx` inside the chat modal. 12 new tests, 221 backend total, frontend `npm run check` and build green.
  - **The premise was wrong and had to be fixed first**: `agent_runs` did NOT already store everything needed. It stores what the agent PROPOSED; nothing stored what the user DECIDED, and Cancel never even reached the server. Hence `agent_action_decisions` (migration 2026-08-23) — see DATABASE_SCHEMA.md for why it is its own table.
  - **Two things worth knowing before opening the screen.** 282 of the 348 rows in `agent_runs` on 2026-08-23 were tagged development test runs, so the history query filters `test_label is null` or the screen shows the developer's noise instead of the user's work. And the newest real run was 2026-08-13 — the screen has almost nothing to show until the agent is used again, which is the honest state, not a defect.
  - **Migration applied and the recording VERIFIED RUNNING 2026-08-24** — a real Confirm changed its task and recorded `confirmed`; a real Cancel recorded `cancelled` and left its task untouched. **The screen was watched too** (owner, 2026-08-24): both conversations listed, Εγκρίθηκε in green and Ακυρώθηκε in red. Only the third status, `Δεν αποφασίστηκε`, has not rendered in front of a person — the 2026-08-11 conversation carries two pre-table proposals and reaches it without any setup.
- **Manual retention cleanup for `agent_runs`** (no automatic job) — deliberately a permanent archive today (see DATABASE_SCHEMA.md); rows are removed only by hand-running SQL if it's ever needed.
- **Admin view over `agent_runs`** — currently queried directly in the Supabase SQL Editor; a proper UI would help non-SQL verification passes.
- **Merge the three `propose_*` agent tools into one `propose_action(action_type, ...)`** — measured at 762 tokens/round for all three combined (paid even on pure reads, where they're never called); a merge should land near ~280, saving ~480 tokens/round (~18% of the fixed prefix). Higher risk than most items here since it's the write path and per-tool docstrings currently help the model pick correctly — do as its own PR with tests 9/10 re-run before/after. (Previously tracked as the open decision in CURRENT_TASK.md; moved here now that file tracks agent-memory verification instead.)
- **Android packaging**: TWA/Bubblewrap. Unblocked now that multi-user is done; the existing web-app OAuth client works for TWA. (PWA can't set native alarms or bypass silent mode — confirmed unsupported.)
- **Custom domain**: attach to Vercel (Settings→Domains) + optionally Render (api.subdomain); doesn't replace the servers.
- **Phone-call escalation (Twilio)**: no permanent free tier (~$1.15/mo number + per-min, charged only on answer). Deferred until after everything else. Viber Bot / WhatsApp calling evaluated and rejected.
- **Limit far-future calendar events**: birthdays pull decades out; cap the forward window.
- **Orphaned Google Calendar events + no retry on a failed delete** (opened 2026-08-07): until `f4dca15`, `delete_calendar_event` never checked its response, so an unknown number of events were left on users' calendars after their tasks were deleted — unknown because nothing was logged while the bug was live. A cleanup would have to work the other way round (list app-tagged events, drop the ones whose `ai_todo_app_task_id` no longer resolves to a task), since the app-side link is gone with the row. Separately, a delete that fails now merely tells the user; retrying it (next scheduler pass, or a queue) is not built.
- **Cleanup (cosmetic)** — **the code half is DONE (2026-08-23)**; what is left is not code. Removed: `pyairtable==3.4.0` from `requirements.txt`, which **nothing imported** — not even `migrate_to_supabase.py`, which talks to Airtable's REST API directly — so it was installed on every deploy for nothing (the file is UTF-16LE with a BOM and CRLF; that survived the edit and was checked byte-wise afterwards). Corrected four comments that stated things which stopped being true: `run_scheduler`'s "this app has no auth system" in `main.py` (untrue since the auth migration — it now says the real reason, that a cron service calls it with no user to authenticate as), and three that still called the database Airtable (`models.py`, `token_tracker.py`, `frontend/src/utils/formatDate.js`). Verified after: 209 backend tests, frontend `npm run check` green.
  - **Deliberately NOT touched**, so the item can be closed without a lie: the `AirtableTaskRepository` class name (its own comment explains why it was kept — renaming reaches into `services.py` and the tests, which is a refactor, not a cleanup), `migrate_to_supabase.py` (a historical one-off, self-documenting), and `AIRTABLE_TOKEN` in the env files (that script still reads it).
  - **Still open, and both are owner actions rather than code**: delete the old Frankfurt Render service, and dedupe any stray `app_settings` rows.
  - (`delete_task`'s "the record is gone from Airtable" was fixed on 2026-08-07 when that endpoint was rewritten to report the calendar outcome.)
- **Sending a task to another person (2026-08-31)** — **the other half of the request that started the workspaces work, deliberately not designed.** The owner asked for multi-user and workspaces together; they are not peers, they stack, and building sharing at the task level before the container existed would have meant building it twice. The container now exists and `workspaces.user_id` was added on day one specifically so this becomes a `workspace_members` table BESIDE the schema rather than a restructuring of it. The questions it has to answer before any code: whose phone rings, whose calendar the event lands on, what the agent sees when asked "what do I have today", and what happens to the ~180 `user_id` filters in `repository.py` that assume one person per row. See "What this deliberately does not do" in `docs/superpowers/specs/2026-08-31-workspaces-and-categories-design.md`.
- **Moving a category between workspaces (2026-09-03)** — parked because there is **no obviously right answer**, not because it is hard. The invariant is that a task's category must live inside the task's own workspace, so moving `μετοχές` from Business to Personal breaks it for every task already filed under it. Three options, each with a real cost: move the tasks too (you asked to move one category and 40 tasks left the workspace), leave them and clear their category (you lose organisation you built), or refuse the move when the category has tasks (honest, but then only empty categories move — i.e. almost never). **Its urgency dropped sharply on 2026-09-03**: every account now has a Business workspace, so an integration's category always has the right home and cannot get stranded in whatever single workspace happened to exist. Pick an option when someone actually needs the move.
- **Migration Part B — dropping `tasks.category` (2026-09-01)** — the old four-word column is invisible in the UI, read by nothing user-facing, and still written by the extractor on every task. The file was deliberately never written: Part A copies and stops so a human can read the counts, and a dropped column cannot be un-dropped. Before it can be written, the extractor must stop filling it and `recurrence_rules.category` needs the same treatment. `ai_suggested_category` stays regardless — it is a frozen archive of what the AI proposed in the old vocabulary and rewriting it would be inventing history.
- **Recurrence rules have workspace/category columns that nothing writes (2026-09-01)** — `recurrence_rules.workspace_id` and `.category_id` exist so the columns are consistent end to end, but a rule still copies its own old `category` TEXT into each occurrence it generates. Wiring them up is a small slice; it was left out because the recurring-task feature's own live gaps (see CURRENT_TASK.md) are still open and adding a second unverified thing on top of an unverified thing makes both harder to attribute.
- **Reordering workspaces and categories by dragging (2026-09-01)** — `position` exists on both tables and is respected on read, but nothing sets it except creation order. Nobody has asked; noted so the column is not mistaken for dead.
- **Reopening a missed recurrence occurrence (2026-09-04)** — the one History state with no way back, and deliberately so. `missed_at` means the occurrence outlived its grace window and closed itself; there is no "un-miss" in the backend, and the day it was for has passed regardless, so a button would either lie about what it did or quietly turn a record of "this did not happen" into a live task with a stale date. The honest version is a different feature: reschedule it to a new date, which is closer to creating a task than to undoing something. Noted so the missing button is not mistaken for the oversight that the completed/rejected ones genuinely were.
- **A "deleted by the system" bucket in History (2026-09-04)** — `repository.delete_tasks_by_ids` still hard-deletes, and its two callers are recurrence housekeeping: editing a rule drops the untouched future occurrences so they can be regenerated on the new schedule, and deleting a rule drops its open ones because with no rule there is no `grace_days` left to close them by. Those rows are regenerable placeholders nobody chose to remove, and soft-deleting them today would fill History with dozens of "διαγράφηκε" entries for acts the user never performed. If that record is ever actually wanted, it needs its own bucket and its own filter, not a reuse of `deleted_at`.
- **Google Calendar: the owner reports a problem, undiagnosed (2026-09-04)** — he said only «έχω θέμα με το Google Calendar» while closing out the Browse work, gave no symptom, and it was **not investigated**. Recorded so the next session starts from "what is actually wrong" rather than assuming. Two things already open in this area that may or may not be the same thing: **orphaned events + no retry on a failed delete** (the item above, open since 2026-08-07), and the **unverified `link_cleared` branch** of `services.restore_task` — restoring a soft-deleted task does not recreate its Google event, the dead link is cleared, and the UI must say so; unit-tested, never run against a real Google account. That last check is parked in CURRENT_TASK.md behind this item, because it cannot tell "our restore is honest" from "the calendar connection is unwell" while the connection is suspect.
